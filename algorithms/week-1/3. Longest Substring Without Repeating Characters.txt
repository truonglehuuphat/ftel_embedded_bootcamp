Given a string s, find the length of the longest substring without duplicate characters.

 

Example 1:

Input: s = "abcabcbb"
Output: 3
Explanation: The answer is "abc", with the length of 3. Note that "bca" and "cab" are also correct answers.
Example 2:

Input: s = "bbbbb"
Output: 1
Explanation: The answer is "b", with the length of 1.
Example 3:

Input: s = "pwwkew"
Output: 3
Explanation: The answer is "wke", with the length of 3.
Notice that the answer must be a substring, "pwke" is a subsequence and not a substring.

abcabcdd
bcabcb
cabc

dùng 2 vòng lập for. sẽ mất thời gian

class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        if (s.length() == 0) return 0;
        string str;
        int len = 0;
        str = str + s[0];
        len = str.length();
        int max = len;
        for(int i = 0; i < s.length(); i++){
            str = s[i];
            for(int j = i+1; j < s.length(); j++){
                if (str.contains(s[j]) == 0){
                    str = str + s[j];
                } else {
                    break;
                }
                len = str.length();
            }
            // cout << str<< endl;
            max = max < len ? len: max;
        }
        // return max = max < len ? len: max;
        return max;
    }
};

Slidingwindown

pwwkew 

điểm bị trùng là w thứ 3 - các mãng trước đó điều thoát ở vị trí thứ 3. 
max sẽ được tính là r - l + 1 

vậy cho r di chuyển từ từ và lấy từng ký tự ra. 
kiểm tra set có chưa giá trị trùng trước khi thêm vào. 
    nếu trùng 
        lấy giá trị của string ở vị trí l 
        xóa nó đi 
        tăng l lên 1;
    kiểm tra lại còn trùng với biến được thêm vào không?

thêm ký tự vào set 
max = r - l + 1

C++
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        set<char> set;
        int l = 0, r = 0, max = 0;
        for(r = 0; r < s.length(); r++){
            char c = s[r];
            while(set.contains(c)){
            // xóa ký tự thứ l ra
                char temp = s[l];
                set.erase(temp);
                l++;
            }
            set.insert(c);
            max = max < ( r - l + 1) ?(r - l + 1) : max;
        }
        return max;
    }
};


ynyo

điểm thoát của mãng đầu tiên ở chỗ y thứ 2
2 điểm di chuyển : r sẽ di chuyển tới điểm y thứ 2
khi gặp điểm trùng l sẽ tăng lên và xóa các vị trí nó đã đi qua trong mãng.
max sẽ được tính là r -l +1 


Java 
class Solution {
    public int lengthOfLongestSubstring(String s) {
        Set<Character> set = new HashSet<>();
        int l = 0,r=0,max = 0;
        for(r =0 ; r < s.length(); r++){
            char c = s.charAt(r);
            while(set.contains(c)){
                set.remove(s.charAt(l));
                l++;
            }
            set.add(c);
            max = Math.max(max, r-l+1);
        }
        return max;
    }
}